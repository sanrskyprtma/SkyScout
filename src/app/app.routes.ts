import { Routes } from '@angular/router';
import { WeatherCheckComponent } from './weather-check/weather-check.component';

export const routes: Routes = [
  { path: '', component: WeatherCheckComponent },
  // tambahkan rute lainnya di sini
];
